# Job service

Build the final integration exercise: an HTTP service that accepts jobs, processes them with bounded concurrency, supports cancellation, returns successes and failures, and protects shared state.
